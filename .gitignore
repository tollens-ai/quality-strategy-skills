# Editor and OS
.DS_Store
*.swp
*~
.vscode/
.idea/

# Host-side agent-harness run artifacts (ephemeral; not part of the skill pack)
.spike-callbacks/
*-completion-callback
*-overnight-latest.json

# Claude Code runtime/host state (handoffs, worktrees, scheduler lock, scratch).
# This is the agent harness's working dir, not part of the published plugin —
# the plugin manifest lives in .claude-plugin/, which is NOT ignored.
.claude/
